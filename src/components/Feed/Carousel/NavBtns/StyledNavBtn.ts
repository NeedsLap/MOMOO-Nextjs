import styled from 'styled-components';

const StyledNavBtn = styled.button`
  position: absolute;
  top: 50%;
  background: rgba(0, 0, 0, 0.4);
  border-radius: 50%;
  box-sizing: content-box;
`;

export default StyledNavBtn;
